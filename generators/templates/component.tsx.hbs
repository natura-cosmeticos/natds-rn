import React from 'react';

import { Wrapper } from './{{ pascalCase name }}.styles';
import { {{ pascalCase name }}Props } from './{{ pascalCase name }}.types';

export const {{ pascalCase name }} = (props: {{ pascalCase name }}Props) => (
    <Wrapper />
);
